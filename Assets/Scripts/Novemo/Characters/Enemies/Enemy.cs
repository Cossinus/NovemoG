using Novemo.Character.Player;
using Novemo.Characters.Player;
using Novemo.Combat;
using UnityEngine;

namespace Novemo.Characters.Enemies
{
    [RequireComponent(typeof(Character))]
    public class Enemy : MonoBehaviour
    {
        /*var playerCombat = _playerManager.player.GetComponent<CharacterCombat>();
        if (playerCombat != null)
        {
            playerCombat.Attack(_characterStats);
        }*/
    }
}
